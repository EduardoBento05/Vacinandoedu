import 'package:flutter/material.dart';

class RecordesPage extends StatelessWidget {
  const RecordesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
